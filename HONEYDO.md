# 🍯 HONEY-DO LIST — Brainrot Report 666

Running list of findings, open items, and ideas. Check things off as they land.
Last updated: 2026-06-05.

---

## 🔴 Integrity / must-fix (the "secretly ethical" promise is now public)

- [ ] **Source EP001's claims.** The episode and the live site both say "sources in
      description" and the ethics table promises *"verifiable sources before publish."*
      EP001's doomscrolling stats + dopamine-hijack claims are currently **unsourced**.
      Add real citations before this episode publishes — the public site is making the
      promise on our behalf.
- [ ] **"Souls saved" counter is mock data.** `assets/site/main.js` climbs a fake number.
      Fine as vibe, but if anyone reads it as real metrics that cuts against the
      Transparency principle. Either keep it obviously playful or replace with real data.

## 🟡 Repo / docs gaps

- [ ] **Scripts referenced in docs but don't exist yet.** `README.md` and
      `departments/creative/NAMING_CONVENTION.md` document a toolchain that isn't in the
      repo. Build them or mark as TODO in the docs:
  - [ ] `scripts/workflow/new-episode.sh`
  - [ ] `departments/creative/scripts/batch-rename.sh`
  - [ ] `departments/creative/scripts/lint-script.sh`
  - [ ] `departments/creative/scripts/generate-script.py`
  - [ ] `departments/creative/scripts/track-assets.py`
- [x] **Fixed:** `scripts/reports/campaign-ethics.sh` syntax error (line ~26 missing
      `echo "` prefix). — done 2026-06-05
- [x] **Added:** `CLAUDE.md` documenting architecture + conventions. — done 2026-06-05

## 🟢 Episode pipeline — EP001

- [x] EP001 draft script filed: `departments/creative/scripts/SCRIPT-EP001-DRAFT-v1.md`
- [ ] **Create EP001 metadata** from `metadata/episodes/template.json` → `metadata/episodes/`
- [ ] **Stage EP001** in the `episodes/` pipeline (`incoming/` or `in-progress/`)
- [ ] Run a handoff log when creative → production: `./scripts/workflow/handoff.sh EP001 creative production`

## 🌐 Website — live at https://epicsereno.github.io/brainrotreport666/

Deployed from the **`Brainrotreport666` branch, root** (no `main`/`gh-pages` branch exists).
Pure HTML/CSS/JS, no build. Files: `index.html`, `assets/site/{styles.css,main.js,episodes.json}`, `.nojekyll`.

Enhancement ideas (all optional):
- [ ] Real episode thumbnails (replace the `EP001` text placeholder in `.ep-thumb`)
- [ ] Custom domain (CNAME) + verify in repo settings
- [ ] Open Graph share image (currently text-only social cards)
- [ ] Split single page → multi-page (`/episodes/`, `/departments/`, etc.) if it grows
- [ ] Analytics dashboard section (filter/sort table, "Latest Drop" spotlight)
- [ ] Wire `episodes.json` to be generated by a repo script instead of hand-edited

## ⚠️ Site vs. repo: department mismatch

- [ ] **`departments.html` lists departments that don't match the repo.** The page (per
      request) shows: Creative, Production, Post-Production, Distribution, Analytics,
      Legal & Compliance, **Ethics Review**, **Talent & Casting**.
      The actual repo `departments/` has: creative, production, post-production,
      distribution, analytics, legal-compliance, **community**, **finance**.
      → Decide the source of truth: add `ethics-review/` + `talent-casting/` dirs to the
      repo, OR swap the site cards back to community + finance. Right now the public page
      describes a structure that doesn't exist on disk (cuts against the Accuracy principle).
- [ ] Note: `index.html`'s in-page department grid still shows the *repo* 8 (incl. Community
      & Finance). The two pages currently disagree — reconcile once the above is decided.

## ❓ Open decisions for the boss

- [ ] **Which GitHub org is canonical?** Work is pushed to `epicsereno/brainrotreport666`
      (origin); the original `bRaiNRoTRePoRt666X/brainrotreport666` is kept as `upstream`
      but `epicsereno` lacks push access there. Decide the home repo.
